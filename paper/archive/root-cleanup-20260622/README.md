# Root Cleanup Archive 2026-06-22

This archive preserves old root-level artifacts that were cluttering the repository root.

## Contents

- `graph/`: legacy plotting helpers and style experiments. This directory was ignored by Git and is not part of the current MRPFuzz paper artifact chain.
- `logs/`: root-level `.log` and `.txt` files, mostly module report merges and static/report-analysis leftovers from March-May 2026.
- `plots/`: root-level `.png` plots generated by older pair-count and coverage experiments.
- `bundles/`: root-level bundles and drafts, including `graph.tar.gz` and the root copy of `MRPFuzz.pdf`.

`config.tar.gz` was inspected during cleanup but left in the repository root because it is a Git-tracked file.

## Current Replacement

Current paper-facing artifacts should be read from:

- `paper/MRPFuzz.pdf`
- `paper/results/mrp-24h-comparison/MANIFEST.md`
- `paper/results/mrp-24h-comparison/20260609-segfuzz0p5x-llm3way-reference-style/`

## Notes

The archived files are ignored by this directory's local `.gitignore`; only this README and the ignore rule are intended to be visible to Git.
